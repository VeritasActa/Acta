{
  "name": "acta",
  "version": "0.1.0",
  "description": "A contestable, checkable, versioned public record",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "verify": "node tools/verify.js"
  },
  "dependencies": {
    "@noble/curves": "^1.9.7"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
